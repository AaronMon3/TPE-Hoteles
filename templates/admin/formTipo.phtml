<?php
$title = $tipo ? 'Editar Tipo' : 'Nuevo Tipo';
require __DIR__ . '/../header.phtml';
?>

    <div class="container">
    <a href="<?= url('admin/tipos') ?>" class="back">← Volver a la lista</a>
    <div class="card" style="margin-top:12px">
            <h1><?= $tipo ? 'Editar' : 'Agregar' ?> Tipo de Habitación</h1>

            <form method="POST" action="<?= (!empty($tipo['id_tipo'])) ? url('admin/tipos/editar/'.(int)$tipo['id_tipo']) : url('admin/tipos/nuevo') ?>" class="form">
                <label>Nombre
                    <input class="control" type="text" name="nombre" value="<?= htmlspecialchars($tipo['nombre'] ?? '') ?>" required>
                </label>

                <label>Descripción
                    <textarea class="control" name="descripcion" rows="4"><?= htmlspecialchars($tipo['descripcion'] ?? '') ?></textarea>
                </label>


                <button type="submit" class="btn btn-primary btn-block"><?= $tipo ? 'Actualizar' : 'Guardar' ?></button>
            </form>

            
        </div>
    </div>
<?php require __DIR__ . '/../footer.phtml'; ?>
