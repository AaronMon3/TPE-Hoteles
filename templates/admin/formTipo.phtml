<?php
$title = $tipo ? 'Editar Tipo' : 'Nuevo Tipo';
require __DIR__ . '/../header.phtml';
?>

    <div class="container">
    <a href="<?= url('admin/tipos') ?>" class="back">← Volver a la lista</a>
    <div class="card" style="margin-top:12px">
            <h1><?= $tipo ? 'Editar' : 'Agregar' ?> Tipo de Habitación</h1>

            <form method="POST" action="<?= $tipo ? url('admin/tipos/editar/'.(int)$tipo['id_tipo']) : url('admin/tipos/nuevo') ?>" enctype="multipart/form-data" class="form">
                <label>Nombre
                    <input class="control" type="text" name="nombre" value="<?= htmlspecialchars($tipo['nombre'] ?? '') ?>" required>
                </label>

                <label>Descripción
                    <textarea class="control" name="descripcion" rows="4"><?= htmlspecialchars($tipo['descripcion'] ?? '') ?></textarea>
                </label>

                <label>URL de la imagen
                    <input class="control" type="url" name="imagen_url" value="<?= htmlspecialchars($tipo['imagen_url'] ?? '') ?>" placeholder="https://ejemplo.com/imagen.jpg">
                </label>

                <button type="submit" class="btn btn-primary btn-block"><?= $tipo ? 'Actualizar' : 'Guardar' ?></button>
            </form>

            <?php if (!empty($tipo['imagen_url'])): ?>
                <p style="margin-top:12px"><img src="<?= htmlspecialchars($tipo['imagen_url']) ?>" alt="Imagen" style="max-width:200px"></p>
            <?php endif; ?>
        </div>
    </div>
<?php require __DIR__ . '/../footer.phtml'; ?>
