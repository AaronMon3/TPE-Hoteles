RewriteEngine On
Options -Indexes

# IMPORTANTE: Si cambias el nombre de la carpeta, actualiza esta línea
# Ejemplo: RewriteBase /mi-carpeta/
RewriteBase /TPE-Hoteles-Main/

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^(.*)$ index.php?action=$1 [QSA,L]
 